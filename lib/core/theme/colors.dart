import 'package:flutter/material.dart';

// ── Backgrounds ───────────────────────────────────────────────────────────────
const Color kBgBase     = Color(0xFF0F1115);
const Color kBgElevated = Color(0xFF171A20);
const Color kBgSurface  = Color(0xFF1D2129);

// ── Accent ────────────────────────────────────────────────────────────────────
const Color kMint       = Color(0xFF7CE8B7);
const Color kMintSoft   = Color(0xFFB7F5D8);
const Color kTeal       = Color(0xFF52C7A7);

// ── Semantic ──────────────────────────────────────────────────────────────────
const Color kPositive   = Color(0xFF7CE8B7);   // owed to you
const Color kNegative   = Color(0xFFFF8A8A);   // you owe
const Color kNeutral    = Color(0xFFC8CDD5);   // settled / inactive

// ── Text ──────────────────────────────────────────────────────────────────────
const Color kTextPrimary   = Color(0xFFF5F7FA);
const Color kTextSecondary = Color(0xFFAAB2BF);
const Color kTextMuted     = Color(0xFF707784);

// ── Divider ───────────────────────────────────────────────────────────────────
const Color kDivider = Color(0xFF2B303A);

// ── Legacy aliases (keeps old references compiling) ──────────────────────────
const Color kBackground = kBgBase;
const Color kSurface    = kBgSurface;
const Color kPrimary    = kBgBase;
const Color kAccent     = kMint;
